import 'package:flutter/material.dart';

class NTextField extends StatelessWidget {
  const NTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return const TextField();
  }
}
